[project]
name = "inventory-collector"
version = "0.1.0"
description = "Cross-platform system inventory collector built with modern Python engineering practices."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "psutil>=7.2.2",
    "psutils>=3.3.16",
]
