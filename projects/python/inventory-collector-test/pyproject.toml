[project]
name = "inventory-collector-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sherif Mohamed", email = "sherif425@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "psutil>=7.2.2",
]

[project.scripts]
inventory-collector-test = "inventory_collector_test:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
